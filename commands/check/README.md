This command uses the `data.Event_Subscription` table (among many others).

The table definition (initialization script) can be found in [event_subscription.sql](../../commands/subscribe/event_subscription.sql).